import { FormErrorsDirective } from './form-errors.directive';

describe('FormErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
